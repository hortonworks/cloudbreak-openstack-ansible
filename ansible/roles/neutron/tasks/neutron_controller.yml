---
- name: install neutron controller components
  apt: name={{ item }} state=latest
  sudo: true
  with_items:
    - neutron-server
    - neutron-plugin-ml2
    - python-neutronclient


- name: create network-init
  sudo: true
  template: src=scripts/network-init.sh.j2 dest=/var/scripts/network-init.sh mode="a+x"

- name: create neutron-entities
  sudo: true
  template: src=scripts/neutron-init.sh.j2 dest=/var/scripts/neutron-init.sh mode="a+x"
  register: neutron_entities

- name: execute neutron-init.sh
  shell: /var/scripts/neutron-init.sh
  when: neutron_entities.changed



